#pragma once
#include <vector>
#include "metrica.hpp"

void selectionSort(std::vector<int> &v, Metrica &m);
void insertionSort(std::vector<int> &v, Metrica &m);
void bubbleSort(std::vector<int> &v, Metrica &m);
void bubbleSortOtimizado(std::vector<int> &v, Metrica &m);
